1.分别执行命令：ls -ldi /etc/. /etc/.. 和
		ls -ldi /. /..
		观察显示的结果.

2.其他两个进程也在运行;

3.指针为形参时，调用函数可以修改其指向的内容，所以要用const来限定，而int型传递的只是值，并不能改变原来数据的内容，所以不必用const限定.

4.调用fflush, fprintf, vprintf函数将会修改errno的值，如果没有保存的话，最终显示的出错消息是不正确的.

5.1970 + (2^31 - 1) / 60 / 60 / 24 / 30 / 12 = 2038年;
  扩展方法：将time_t数据类型定为64位整型，并且需要重新编译应用程序.

6.(2^31 - 1) / 100 / 60 / 60 / 24 = 248.
